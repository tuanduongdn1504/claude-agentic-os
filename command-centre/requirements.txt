fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
pyyaml>=6.0
requests>=2.31
python-dotenv>=1.0
# Optional — only exercised when CC_USE_FSEVENTS=1 opts in to the experimental
# sub-2s JSONL watcher. Default 120s polling loop has no runtime use for it.
watchdog>=4.0
